{
  "name": "MyTelegramOrgRoBot",
  "description": "get your APP ID and API HASH inside Telegram",
  "keywords": [
    "telegram",
    "best",
    "app",
    "scrapper",
    "3"
  ],
  "repository": "https://github.com/SpEcHiDe/MyTelegramOrgRoBot",
  "success_url": "https://telegram.dog/SpEcHlDe",
  "website": "https://github.com/SpEcHiDe/MyTelegramOrgRoBot",
  "env": {
    "WEBHOOK": {
      "description": "Mengatur ini ke apa pun akan memungkinkan WebHook ketika dalam mode Env",
      "value": "ANYTHING"
    },
    "TG_BOT_TOKEN": {
      "description": "Token bot Anda, sebagai string."
    },
    "URL": {
      "description": "The Heroku App URL similar to https://<appname>.herokuapp.com/",
      "value": "https://example.com/"
    },
    "APP_TITLE": {
      "description": "Tentukan judul aplikasi khusus",
      "required": false
    },
    "APP_SHORT_NAME": {
      "description": "Tentukan nama pendek alfamumerik kustom (5-32 karakter)",
      "required": false
    },
    "APP_URL": {
      "description": "specify a custom app URL",
      "required": false
    },
    "APP_DESCRIPTION": {
      "description": "specify a custom app description",
      "required": false
    },
    "FTEXT": {
      "description": "the footer text that will be seen in the bot message, after the final edit",
      "required": false
    },
    "START_TEXT": {
      "description": "Teks Mulai dari Bot Telegram",
      "required": false
    },
    "AFTER_RECVD_CODE_TEXT": {
      "description": "the message that should be displayed before receiving the code from the user",
      "required": false
    },
    "BEFORE_SUCC_LOGIN": {
      "description": "a temporary message that will be displayed after receiving the code, and before the validations",
      "required": false
    },
    "ERRED_PAGE": {
      "description": "a string which will **never** occur, but still exists?",
      "required": false
    },
    "CANCELLED_MESG": {
      "description": "a message that should be shown on /cancel",
      "required": false
    },
    "IN_VALID_CODE_PVDED": {
      "description": "a string which will **never** occur, but still exists?",
      "required": false
    },
    "IN_VALID_PHNO_PVDED": {
      "description": "Pesan yang akan ditampilkan jika logika `get_phno_imn_ges`, istirahat",
      "required": false
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "free"
    }
  },
  "stack": "heroku-20"
}
